import wollok.game.*
import heroes.*

program juego {
	game.title("Perdidigma")
	game.addVisual(caballero)
	game.addVisual(leproso)
	game.addVisual(doctor)

	game.boardGround("fondito.png")
	game.cellSize(100)
	game.width(50)
	game.height(50)
	game.start()

	//aca irian los iconos de los chabones
}

