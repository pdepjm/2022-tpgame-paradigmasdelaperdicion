import wollok.game.*
import teclado.*
import heroes.*
import movimiento.*
import enemigos.*
import turnos.*

//TESTS DE MOVIMIENTO Y POSICION
test "si el lider esta en la posicion (20, 5) y se mueve una vez para la derecha, el lider queda en la posicion (21, 5)" {
	lider.mover(derecha, 1)
	assert.equals(game.at(21, 5), lider.position())
}

test "si el lider esta en la posicion (20, 5) y se mueve una vez para la izquierda, el lider queda en la posicion (19, 5)" {
	lider.mover(izquierda, 1)
	assert.equals(game.at(19, 5), lider.position())
}

test "si el seguidor 1 esta en la posicion (14, 5) y se mueve una vez para la derecha, el lider queda en la posicion (15, 5)" {
	lider.mover(derecha, 1)
	assert.equals(game.at(15, 5), seguidor1.position())
}

//hay que hacer los nuevos tests de posicion y los tests de la cantidad de danio\

test "Ataque del caballero al ectoplasma"{
	
	const caballero2 = new Caballero()
		
	turno.enemigo(ectoplasma)
	
	assert.equals(1000, ectoplasma.vida())	
	
	caballero2.atacar(cascotaso)
	
	assert.equals(960, ectoplasma.vida())	
}
test "Recibir ataque caballero glotoneria"{
	
	const caballero2 = new Caballero()
		
	assert.equals(100, caballero2.vida())	
	
	caballero2.recibirAtaque(glotoneria)
	
	assert.equals(80, caballero2.vida())	
}

test "Ataque del doctor al ectoplasma"{
	
	const doctor2 = new Doctor()
		
	turno.enemigo(ectoplasma)
	
	assert.equals(1000, ectoplasma.vida())	
	
	doctor2.atacar(cascotaso)
	
	assert.equals(980, ectoplasma.vida())	
}
test "Recibir ataque brujo glotoneria"{
	
	const doctor2 = new Doctor()
		
	assert.equals(100, doctor2.vida())	
	
	doctor2.recibirAtaque(glotoneria)
	
	assert.equals(90, doctor2.vida())	
}
test "Ataque del leproso al ectoplasma"{
	
	const leproso2 = new Leproso()
		
	turno.enemigo(ectoplasma)
	
	assert.equals(1000, ectoplasma.vida())	
	
	leproso2.atacar(cascotaso)
	
	assert.equals(970, ectoplasma.vida())	
}
test "Recibir ataque leproso glotoneria"{
	
	const leproso2 = new Leproso()
		
	assert.equals(100, leproso2.vida())	
	
	leproso2.recibirAtaque(glotoneria)
	
	assert.equals(90, leproso2.vida())	
}














